PRO-CRAWL OS PROMPT_DB

This folder is for exported or manually stored script presets.
The app itself saves dynamic presets in browser localStorage so they persist per-device/per-browser.

Built-in preset: Basalt Engine Alpha Stream.
Saved user preset storage key: pro_crawl_user_presets_v1
Current draft storage key: pro_crawl_script

You can manually keep .md/.txt script backups here if you want to move presets between machines.
